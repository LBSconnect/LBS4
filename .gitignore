node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# E2E test secrets — never commit
.env.test

# Playwright output
playwright-report/
test-results/

# Private I-9 portal file storage — never publicly served, never committed
server/private-uploads/